#1
<p>The Python function <code>len()</code> is used to find the length of a value.</p>

#2
<pre> def factorial(n): if n == 0: return 1 return n * factorial(n - 1) </pre>

#3
<p>Press <kbd>Ctrl + C</kbd> to copy the selected text.</p>

#4
<p>The program output is <samp>Hello, World!</samp>.</p>

#5
<p>The formula is F = <var>m</var>a, where <var>m</var> is the mass.</p>

#6
<style>
pre {
    background: #1e1e1e;
    color: white;
    padding: 15px;
    border-radius: 8px;
    font-family: monospace;
}
</style>

<pre>
for i in range(5):
    print(i)
</pre>

#7
<p>Use <code>color: red;</code> to change the text color.</p>

<pre>
git status
git add .
git commit -m "Update"
git push
</pre>

#8
<p>Type <kbd>pwd</kbd> in the terminal, and the output may be <samp>/home/user/project</samp>.</p>

#9
<style>
kbd {
    background: #f2f2f2;
    border: 1px solid #999;
    padding: 5px;
    border-radius: 5px;
    font-family: monospace;
}
</style>

<p>Press <kbd>Enter</kbd> to continue.</p>

#10
<p>Use <code>test -f filename</code> to check whether a file exists.</p>

#11
<pre>
+-------+
| Hello |
+-------+
</pre>

#12
<p>The variable <var>count</var> has the assignment <code>count = 10</code>.</p>

#13
<p>Use <code>print("Hello")</code> to display a message.</p>

<p>Press <kbd>Ctrl + S</kbd> to save the file.</p>

<p>The output is <samp>Hello</samp>.</p>

#14
<!DOCTYPE html>

<html>
<head>
    <style>
        kbd {
            background: #f2f2f2;
            border: 1px solid #999;
            padding: 5px;
            border-radius: 5px;
            font-family: monospace;
        }
    pre {
        background: #1e1e1e;
        color: white;
        padding: 15px;
    }
</style>
</head>

<body>

<p>Press <kbd>Enter</kbd> to continue.</p>

<pre>
print("Hello, World!")
print("Welcome!")
</pre>

</body>
</html>

#15
<!DOCTYPE html>

<html>
<head>
    <title>Computer Code</title>
<style>
    pre {
        background: #1e1e1e;
        color: white;
        padding: 15px;
        border-radius: 8px;
    }

    kbd {
        background: #f2f2f2;
        border: 1px solid #999;
        padding: 5px;
        border-radius: 5px;
        font-family: monospace;
    }
</style>
</head>

<body>

<p>Use <code>font-size: 20px;</code> to change text size.</p>

<pre>
name = "Rahul"
print(name)
</pre>

<p>Press <kbd>Ctrl + S</kbd> to save the file.</p>

<p>The output is <samp>Hello, World!</samp>.</p>

<p>The variable <var>x</var> stores a value.</p>

</body>
</html>
